<div class="row">
  <div class="col-md-6 col-md-offset-3">
<%= form_for(@change_evaluation) do |f| %>
  <% if @change_evaluation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@change_evaluation.errors.count, "error") %> prohibited this change_evaluation from being saved:</h2>

      <ul>
      <% @change_evaluation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


    <%= f.label :rate %><br>
    <%= f.number_field :rate, class: 'form-control' %>

    <%= f.label :description %><br>
    <%= f.text_area :description, class: 'form-control' %>

    <%= f.label :change_request %><br>
    <%= f.select :change_request_id, options_from_collection_for_select(@change_requests, 'id', 'name'), class: 'form-control'%>

    <%= f.label :User %><br>
    <%= f.select :user_id, options_from_collection_for_select(@users, 'id', 'name'), class: 'form-control'%>

    <%= f.submit "Kreiraj", class: "btn btn-primary" %>

<% end %>
</div>
</div>

